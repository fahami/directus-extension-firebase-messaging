# Firebase Cloud Messaging Configuration
# Path to your Firebase service account JSON file
FIREBASE_CREDENTIAL_PATH=/path/to/firebase-service-account.json

# Example: For development, you might use a relative path
# FIREBASE_CREDENTIAL_PATH=./config/firebase-adminsdk.json

# Example: For production, use an absolute path
# FIREBASE_CREDENTIAL_PATH=/var/directus/config/firebase-credentials.json
